<Page
    x:Class="Save_Me.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Save_Me"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Background="CadetBlue" Height="400" Width="auto" Grid.Row="0" Grid.Column="0" >
        <TextBlock Text="Welcome" FontFamily="Segoe UI" FontSize="72" FontWeight="ExtraLight" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock Name="Description"/>
        </StackPanel>
        <Button x:Name="NextButton" Tapped="NextButton_Tapped" Foreground="White" FontFamily="Segoe MDL2 Assets" Content="&#xE72A;" FontSize="28" HorizontalAlignment="Right" VerticalAlignment="Bottom" Background="CadetBlue" Margin="0,0,10,120"/>
    </Grid>
</Page>
